noinst_PROGRAMS		= mic mis		# Build, but don't install

mic_SOURCES		= mic.c
mic_CFLAGS		= -g
mic_LDADD		= ../src/libiperf.la
mic_LDFLAGS		= -g

mis_SOURCES		= mis.c
mis_CFLAGS		= -g
mis_LDADD		= ../src/libiperf.la
mis_LDFLAGS		= -g

